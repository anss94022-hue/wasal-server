import { describe, expect, it } from "vitest";

describe("messages realtime", () => {
  it("should create a message and emit it to the conversation", async () => {
    expect(true).toBe(false);
  });
});
